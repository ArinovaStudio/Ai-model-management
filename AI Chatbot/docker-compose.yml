version: "3.9"

services:
  chatbot:
    build: .
    container_name: ai-chatbot
    ports:
      - "8000:8000"
    restart: always
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1