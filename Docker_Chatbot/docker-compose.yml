version: '3.8'

services:
  app:
    build: .
    container_name: ai_chatbot_app
    ports:
      - "8000:8000"
    environment:
      # This tells Docker to look for DATABASE_URL in your .env file
      - DATABASE_URL=${DATABASE_URL}
    restart: always
